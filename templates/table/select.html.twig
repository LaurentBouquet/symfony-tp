{% extends 'base.html.twig' %}

{% block title %}Hello TableController!{% endblock %}

{% block body %}

<div class="example-wrapper">
    <h1>Select</h1>

    {# {{ form(formulaire) }} #}

    {{ form_start(formulaire)}}

    {{ form_widget(formulaire.table_number)}}
    {{ form_widget(formulaire.Choisir)}}

    {{ form_end(formulaire)}}
    

</div>
{% endblock %}
